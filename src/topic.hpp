// Define quiz_name to topic map
// very unelegant this should be fixed by
// converting the question db to a proper db (JSON ?)

map<string, string> topic_map = {
  { "quiz_name", { "Suggested topics" } }
};
